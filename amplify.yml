version: 1
applications:
  - frontend:
      phases:
        preBuild:
          commands:
            - npm install -g pnpm@9
            - env | grep -E "^(NEXTAUTH_|AUTH_|GOOGLE_|DATABASE_|SLACK_|S3_|NEXT_PUBLIC_)" >> .env.production
            - pnpm install --no-frozen-lockfile
        build:
          commands:
            - pnpm build
      artifacts:
        baseDirectory: .next
        files:
          - '**/*'
      cache:
        paths:
          - .next/cache/**/*
          - node_modules/**/*
